# Frozen governing reference

These two Python files are unchanged copies of the governing handover's
`reference/` directory. They remain version 2.0 boundary examples and are
executed independently of the application 2.1 suite. Refresh only when the
governing reference changes, preserving its provenance. Run:

`uv run python -m unittest discover -s reference -q`
